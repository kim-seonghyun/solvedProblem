function solution(s) {
    s = s.split('').map((el)=>{
        return el.charCodeAt();
    }).sort(function(a,b){return b-a}).map((el)=>{
        return String.fromCharCode(el);
    });
    return s.join('');
}

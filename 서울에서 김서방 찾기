function solution(seoul) {
    let reg = /Kim/g;
    for(let i in seoul){
        if(reg.test(seoul[i])){
            return `김서방은 ${i}에 있다`
        }
    }
}

//Array.protoType.indexOf()로 푸는게 더 간편함


function solution(seoul) {
    return `김서방은 ${seoul.indexOf("Kim")}에 있다`;
}
